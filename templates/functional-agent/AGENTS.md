# AGENTS.md - ${AGENT_NAME} 任务契约

<!-- agent-type: functional -->

## 任务定义

**任务名称**: ${AGENT_NAME}

**角色**: ${ROLE}

## 输入

- **格式**: 
- **必需字段**: 
- **可选字段**: 

## 输出

- **格式**: 
- **成功标准**: 
- **失败处理**: 

## 执行规则

1. 接收输入
2. 验证输入格式
3. 执行核心逻辑
4. 返回结果
5. 退出

## 职责边界

- ✅ 只处理定义范围内的任务
- ❌ 不保留状态
- ❌ 不主动扩展功能
- ❌ 不与其他系统交互（除非明确定义）

## 错误处理

- 输入验证失败 → 返回错误信息并退出
- 执行失败 → 记录错误并返回失败状态
- 超时 → 立即终止并返回超时状态
